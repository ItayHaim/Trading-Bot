export enum OrderStatus {
    Open = 'open',
    Closed = 'closed',
    Canceled = 'canceled'
}

export enum OrderType {
    StopLoss = 'stopLoss',
    TakeProfit = 'takeProfit'
}

export enum BuyOrSell {
    Sell = 'sell',
    Buy = 'buy'
}