export enum OrderStatus {
    Open = 'open',
    Closed = 'closed',
    Canceled = 'canceled'
}

export enum OrderType {
    StopLoss = 'stopLoss',
    TakeProfit = 'takeProfit'
}

export enum BuyOrSell {
    Sell = 'sell',
    Buy = 'buy'
}

export enum Crosses {
    CrossDown = 'crossDown',
    CrossUp = 'crossUp'
}

export enum SMACrossTypes {
    '3TO8' = '3TO8',
    '8TO13' = '8TO13'
}
